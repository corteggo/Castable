// swift-tools-version:5.2

import PackageDescription

let package = Package(
    name: "Castable",
    products: [
        .library(
            name: "Castable",
            targets: ["Castable"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "Castable",
            dependencies: [])
    ]
)
